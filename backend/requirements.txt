# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20
aiofiles==24.1.0
httpx==0.28.1
websockets==14.1

# --- Models / HuggingFace ---
huggingface_hub==0.27.1
# Optional Rust accelerator: much faster model downloads (VIBEMCP_HF_*).
hf_transfer==0.1.8

# --- Vector DB + embeddings (fastembed = ONNX, ARM-friendly, no torch) ---
qdrant-client[fastembed]==1.12.2

# --- GPU / system monitoring ---
nvidia-ml-py==12.560.30
psutil==6.1.1

# --- MCP server ---
mcp>=1.9

# --- Document parsing for RAG ---
pypdf==5.1.0
python-docx==1.1.2

# --- Studio 3D (optional, installed via scripts/setup_studio3d.sh) ---
# Image upload handling needs Pillow; the heavy diffusers/torch + TRELLIS.2
# stacks are intentionally NOT pinned here (large, GPU/arch specific) and are
# loaded lazily by app/services/studio3d.py.
Pillow>=10.0
